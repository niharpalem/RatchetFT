# RatchetFT — runtime dependencies
# Install into the uv venv with:  uv pip install -r requirements.txt
# (this .venv has no pip; use `uv pip`, not plain pip)

# Web server (FastAPI + HTMX + Jinja2)
fastapi>=0.136
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.20      # form posts (task input, predict)

# Agent brain = the local `claude` CLI (subprocess) — no SDK / API key.
# It must be on PATH:  which claude

# Real local training + inference (LocalTrainer, on Apple Silicon MPS / CUDA)
torch>=2.4
transformers>=4.55            # LFM2 + modern model support
peft>=0.11                    # LoRA adapters
datasets>=2.20                # HuggingFace dataset loading (+ huggingface_hub)
accelerate>=0.30
huggingface_hub>=0.24         # grounded dataset/model search

# Optional — cloud GPU backend (only for RATCHETFT_BACKEND=modal)
# uv pip install modal ; .venv/bin/modal setup ; modal secret create huggingface HF_TOKEN=...
modal>=1.4

# NOTE on the corp proxy: dataset/model downloads work from YOUR terminal but are
# blocked inside the Claude Code sandbox. Once cached, load offline with HF_HUB_OFFLINE=1.
