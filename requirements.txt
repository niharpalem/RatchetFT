# RatchetFT — runtime dependencies
# Install:  uv pip install -r requirements.txt

# Web server (FastAPI + HTMX + Jinja2)
fastapi>=0.136
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.20
python-dotenv>=1.0            # loads configuration/secrets from .env

# Agent brain: the local `claude` CLI (invoked as a subprocess). Must be on PATH.

# Training + inference (LoRA fine-tuning on Apple Silicon MPS / CUDA)
torch>=2.4
transformers>=4.55
peft>=0.11
datasets>=2.20
accelerate>=0.30
huggingface_hub>=0.24

# Optional — cloud GPU backend (enable with RATCHETFT_BACKEND=modal)
modal>=1.4

# Configuration: copy .env.example to .env and set your tokens. .env is never committed.
